spring:
  application:
    name: nexttrip-backend
  data:
    mongodb:
      uri: mongodb://localhost:27017/next_trip

server:
  port: 8080

# Comma-separated browser origins allowed to call this API (required when using credentials).
cors:
  allowed-origins: "http://localhost:4200,http://localhost:3000,http://localhost:5173"
